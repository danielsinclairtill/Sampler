query ItemList($first: Int, $after: String) {
  posts(first: $first, after: $after) {
    nodes {
        ...ItemCompactGraph
    }
    totalCount
    pageInfo {
      endCursor
      hasNextPage
      hasPreviousPage
      startCursor
    }
  }
}
